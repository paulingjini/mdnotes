<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Floppy disk outline -->
  <rect x="3" y="2" width="14" height="16" rx="1.5" stroke="currentColor" stroke-width="1.5" fill="none"/>

  <!-- Disk detail (top slot) -->
  <path d="M 3 5 L 17 5" stroke="currentColor" stroke-width="1.5"/>

  <!-- Save indicator (bottom) -->
  <rect x="6" y="11" width="8" height="5" rx="0.5" fill="currentColor"/>

  <!-- Disk detail (side notch) -->
  <rect x="13" y="2" width="2" height="3" fill="currentColor"/>
</svg>
